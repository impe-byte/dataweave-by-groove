import { describe, it, expect } from 'vitest';

describe('Button Component - Sanity Check', () => {
  it('should pass the test runner check', () => {
    // Questo è un placeholder per sbloccare Vitest.
    // I test reali sui componenti React verranno implementati 
    // dopo il setup di jsdom/happy-dom.
    expect(true).toBe(true);
  });
});
